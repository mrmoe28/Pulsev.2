# Disable yarn and force npm usage
# This prevents Vercel from auto-detecting yarn/pnpm

# Force npm package manager
packageManager: npm

# Disable yarn features  
enableTelemetry: false
preferInteractive: false

# Node linker
nodeLinker: node-modules
